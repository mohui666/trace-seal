# TraceSeal v0.2.0 Validation

Validation date: 2026-06-18 (Asia/Shanghai)

## Status legend

- **Executed locally**: reproduced on the current Windows 11 host.
- **Clean VM executed**: reproduced in an independent Windows environment without developer tools.
- **Not executed**: no evidence was available or the required environment was unavailable.
- **Failed**: the step was run and did not meet its acceptance criterion.

## Git state

- Initial branch: `test/v0.2-clean-windows` at `2c7c247e73e302ad23681b43dc720169b5208cfe`.
- Initial working tree: not clean; it contained an uncommitted Squirrel lifecycle fix in `desktop/electron/src/main.ts`, `desktop/electron/src/squirrel.ts`, and `desktop/electron/tests/squirrel.test.ts`.
- Initial PR #2 state: open, mergeable, Windows Build successful.
- PR #2 result: squash merged into `main` as `dd7cb45a402269f81f9e41dbc45ddd2e54fb6fbc`.
- Validation fix commit: `a4438e10290f9a2103622c8eaa0eb5c0eac8bc6a`.
- Validation PR: https://github.com/mohui666/trace-seal/pull/3
- Validation PR result: Windows Build passed; squash merged into `main` as `c18c1afaf67db1a5a65081f5c040da323ec0ad29`.
- Validated release-code main SHA: `c18c1afaf67db1a5a65081f5c040da323ec0ad29` (the later report-only merge is not part of the installer build).
- Old branch cleanup: `codex/backup-local-main-pre-pr` and `codex/v0.2-release-candidate` were already absent locally and remotely. The merged `test/v0.2-clean-windows` remote branch was removed by the PR merge.
- Initial tag / Release state: no Git tag and no GitHub Release.
- Final tag / Release state: none created, because clean Windows validation is incomplete.

## Build

- Python: **Executed locally — PASS**, 13/13 tests.
- Renderer: **Executed locally — PASS**, typecheck, 96/96 tests, production build.
- Electron: **Executed locally — PASS**, typecheck, 45/45 tests (33 existing plus 12 Squirrel lifecycle tests).
- Full build: **Executed locally — PASS** using `scripts/build-windows.ps1` after deleting the previous `desktop/electron/out` directory.
- Installer: `C:\Users\mohui666\Documents\projectA\trace-seal\desktop\electron\out\make\squirrel.windows\x64\TraceSeal-Setup.exe`
- SHA256: `62388083e73c00f4df78d1fc826b70c08e7f3cd70f03448e65129dc4025a02f8`
- SHA256 manifest: `C:\Users\mohui666\Documents\projectA\trace-seal\desktop\electron\out\make\squirrel.windows\x64\SHA256SUMS.txt`
- Source verification: **Executed locally — PASS** on clean `main`.
- Artifacts verification: **Executed locally — PASS** on clean `main`; installer, manifest, renderer index, bundled Core, version consistency, and SHA256 were all checked.

## Local installed validation

- Install: **Executed locally — PASS**; final main installer exited 0 and installed version 0.2.0.
- Launch: **Executed locally — PASS** using the installed Squirrel stub. Start Menu and Desktop shortcuts were created; a literal Start Menu click was not executed because the Windows UI automation runtime was unavailable.
- Live Electron: **Executed locally — PASS**; installed renderer displayed `Live Electron`.
- App version: **Executed locally — PASS**; installed renderer displayed `v0.2.0` and the executable file version was 0.2.0.
- Bundled Core: **Executed locally — PASS**; runtime reported `resources\traceseal-core\traceseal-core.exe` and `bundled traceseal-core`.
- No system Python dependency: **Executed locally — PASS by runtime command inspection**; installed data requests used the bundled executable, not the host Python command.
- No Node.js dependency: **Executed locally — PASS by packaged runtime inspection**; the installed app loaded packaged Electron/renderer resources and did not invoke a system Node command. The host itself has Node installed, so this is not a clean-machine proof.
- Workspace selection: **Partially executed locally**; saved valid and invalid workspaces were loaded and rendered. Native directory-picker interaction was not executed because the Windows UI automation runtime was unavailable.
- Runs: **Executed locally — PASS**; 20 runs loaded.
- Run Detail: **Executed locally — PASS**.
- Explain: **Executed locally — PASS**.
- Policy: **Executed locally — PASS**.
- Workspace switch: **Executed locally — PASS via persisted settings and restart**; the runtime root changed to the Unicode/space path. Native picker switching was not executed.
- Workspace clear: **Executed locally — PASS** through the installed Electron bridge.
- Clear then restart: **Executed locally — PASS**; `settings.json` was absent and first-start guidance appeared after restart.
- Unicode and spaces: **Executed locally — PASS** using `C:\Users\mohui666\AppData\Local\Temp\TraceSeal v0.2 验收\中文 工作区`; 20 runs loaded and the path was displayed.
- Settings persistence: **Executed locally — PASS** across restart and uninstall/reinstall.
- Corrupt settings: **Executed locally — PASS**; invalid JSON recovered to first-start guidance without crashing.
- Invalid workspace: **Executed locally — PASS**; the missing path was retained and rendered as invalid with recovery guidance.
- Uninstall/reinstall: **Executed locally — PASS**; uninstall exited 0, removed the app stub and both shortcuts, and reinstall exited 0 and restored the app, packaged resources, and both shortcuts. Squirrel retained only its updater skeleton marked `.dead` after uninstall.
- SmartScreen: **Not executed**; silent local installation did not provide a SmartScreen result.

## Clean Windows validation

- Environment: **Not executed**. No Windows Sandbox executable, Hyper-V PowerShell module, VirtualBox, or VMware entry point was available on this host. Windows optional-feature inspection required elevation and was not changed.
- Python installed: **Not tested in a clean environment**. The current host has Python installed.
- Node.js installed: **Not tested in a clean environment**. The current host has Node.js installed.
- Git installed: **Not tested in a clean environment**. The current host has Git installed.
- Result: **INCOMPLETE**.

## Issues

### Squirrel lifecycle and shortcuts

- Issue: the pre-fix installed build did not handle Squirrel lifecycle arguments; an uninstall attempt exited `-1`, and installation did not reliably create shortcuts.
- Severity: release blocking for the Windows installer.
- Fix: added early Squirrel install/update/uninstall/obsolete handling, explicit shortcut creation/removal through `Update.exe`, and 12 unit tests.
- Retest: **PASS**; install created two shortcuts, uninstall exited 0 and removed the app stub/shortcuts, and reinstall restored them.

### Release label

- Issue: the renderer displayed `v0.2.0 RC`.
- Severity: release blocking presentation mismatch.
- Fix: changed the label to `v0.2.0`.
- Retest: **PASS** in the installed application.

### Windows UI automation

- Issue: the local Computer Use runtime failed to load because the bundled `@oai/sky` package did not export a required subpath.
- Severity: validation tooling limitation.
- Fix: none in the TraceSeal repository. Installation and renderer behavior were validated through installer exit codes, filesystem/process inspection, Electron CDP, DOM text, and the exposed Electron bridge.
- Retest: native Start Menu click and directory-picker interaction remain **not executed**.

## Final result

**INCOMPLETE**

The source, build, artifacts, and extensive local installed-app checks pass. A formal v0.2.0 tag and GitHub Release must not be created until an independent clean Windows environment (without Python, Node.js, and Git) completes installation, launch, bundled-Core, SmartScreen, workspace, uninstall, and reinstall validation.
